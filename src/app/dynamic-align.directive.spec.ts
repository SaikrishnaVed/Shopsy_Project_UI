import { DynamicAlignDirective } from './dynamic-align.directive';

describe('DynamicAlignDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicAlignDirective();
    expect(directive).toBeTruthy();
  });
});
